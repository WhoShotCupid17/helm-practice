apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx-svc
spec:
  type: ClusterIP
  selector:
    app: nginx-svc
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80 
